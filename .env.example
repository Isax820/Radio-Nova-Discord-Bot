# Copy this file to .env and fill the values
# Your Discord bot token (replace with your bot token)
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE

# Stream URL for Radio Nova (main station). One publicly-known stream endpoint is provided below.
# If this stops working later, you can replace it with another from the list in README or from the radio-browser API.
STREAM_URL=https://novazz.ice.infomaniak.ch/novazz-128.mp3

# Optional: default volume (0.0 - 2.0)
VOLUME=0.6

# Optional: for faster slash command registration during development, set your test server (guild) ID.
# If GUILD_ID is set, the bot will sync slash commands to that guild on startup (immediate). Leave empty to
# register globally (can take up to an hour to appear).
GUILD_ID=1436774736603648113
